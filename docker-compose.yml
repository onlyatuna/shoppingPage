services:
  # 👇 [新增] Caddy 服務 (負責 HTTPS)
  caddy:
    image: caddy:latest
    container_name: shopping-caddy
    restart: always
    ports:
      - "80:80" # HTTP (自動轉 HTTPS)
      - "443:443" # HTTPS
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile # 掛載設定檔
      - ./certs:/etc/caddy/certs # 新增掛載憑證資料夾
      - caddy_data:/data # 持久化憑證
      - caddy_config:/config
    depends_on:
      - app
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
  app:
    # 1. [修正] 改用 GHCR Image (全小寫) 並支援變數覆蓋
    # 讓 EC2 或 DEV 直接下載編譯好的檔案，不要自己 Build
    image: ${IMAGE_TAG:-ghcr.io/onlyatuna/shoppingpage:latest}
    container_name: shopping-app
    restart: always

    # 2. [SECURITY] 內部網路隔離
    # 不再直接對外開放 3000 埠口，僅透過 Caddy 進行反向代理訪問
    expose:
      - "3000"

    # 3. [修正] 讀取 CI/CD 生成的 .env 檔案
    # 這樣就不用把密碼寫死在這裡，而且能讀到最新的 GitHub Secrets
    env_file:
      - .env

    depends_on:
      - db

    # 4. [保留] 資源限制 (做得好！這能防止當機)
    deploy:
      resources:
        limits:
          cpus: '0.80'
          memory: 1G

    # 5. [新增] 健全狀況檢查 (搭配 CI/CD 的 --wait 強化部署可靠性)
    healthcheck:
      test: [ "CMD-SHELL", "node -e \"require('http').get('http://localhost:3000/api/health', (r) => {if (r.statusCode !== 200) process.exit(1);})\"" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  db:
    image: mysql:8.0
    container_name: shopping-db
    restart: always
    environment:
      # 5. [修正] 密碼改用變數 (會自動讀取 .env 中的 DATABASE_PASSWORD)
      # 為了相容您原本的 .env 生成腳本，我們稍後需確認 .env 有沒有這個變數
      # 如果沒有，暫時寫死也可以，但建議用變數
      MYSQL_ROOT_PASSWORD: ${DATABASE_PASSWORD:-Qq940628}
      MYSQL_DATABASE: shop_db
    volumes:
      - mysql_data:/var/lib/mysql
      # 加入這個設定以優化 MySQL 記憶體使用 (對小機器很重要)
    command: --default-authentication-plugin=mysql_native_password --innodb-buffer-pool-size=256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  mysql_data:
  caddy_data: # [新增]
  caddy_config: # [新增]
